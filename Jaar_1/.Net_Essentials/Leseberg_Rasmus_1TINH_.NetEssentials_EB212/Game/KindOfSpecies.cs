namespace Game
{
    public enum KindOfSpecies
    {
        Carrot, EarthWorm, Acorn
        // carrot =  wortel voedsel voor konijn (rabbit)
        // earthworm =  regenworm voedsel voor een egel(hedgehog)
        // acorn = eikel voedsel voor een eekhoorn (squirrel)
    }
}
