---
- hosts: all
  become: true
  tasks:
    - name: install firewalld
      yum:
        name: firewalld
        state: latest

    - name: Set firewall rule for HTTP traffic
      firewalld:
        state: enabled
        immediate: yes
        port: "{{ port }}"
        zone: public
